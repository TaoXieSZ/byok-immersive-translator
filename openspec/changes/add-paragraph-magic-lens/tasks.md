## 1. 锁定选区与上下文基线

- [x] 1.1 为普通段落、列表、标题、长段落、跨容器选区以及排除区域建立确定性的选区 fixture
- [x] 1.2 先编写选区规范化、1–2000 字符限制、有意义字符判断和安全祖先过滤的失败测试
- [x] 1.3 先编写覆盖选区的 4000 字符上下文窗口、空白规范化和安全退化行为测试
- [x] 1.4 实现纯函数选区快照与上下文提取模块，使 1.2–1.3 的测试通过
- [x] 1.5 运行 `npm run verify`，确认选区基线未影响现有整页提取和格式保留

## 2. 扩展安全消息与选区缓存协议

- [x] 2.1 先为选区开始、流式片段、完成、错误和取消消息编写严格键集合与长度边界测试
- [x] 2.2 增加选区消息类型和验证器，拒绝 Provider、URL、模型、请求头、并发及其他越权覆盖
- [x] 2.3 先为选区模式、选中文字哈希、上下文哈希、Provider、语言和 Prompt/schema 维度编写缓存隔离测试
- [x] 2.4 实现选区专用缓存键与完整结果存储，确保缓存记录不保存选区和上下文明文
- [x] 2.5 增加 `bypassCache` 严格布尔验证及绕过读取但允许验证后覆盖写入的测试
- [x] 2.6 运行 `npm run verify`，确认共享协议和原有页面缓存保持兼容

## 3. 实现可信后台选区翻译

- [x] 3.1 先为“只翻译 selection、context 仅用于消歧”的专用 Prompt 和纯文本输出编写适配器测试
- [x] 3.2 实现选区单段流式/非流式请求适配，复用当前 Provider、共享调度限制和现有错误分类
- [x] 3.3 在 translation service 中实现选区缓存读取、远端请求、验证完成后写入和显式重新翻译
- [x] 3.4 按独立 `requestId` 实现选区取消和迟到后台结果隔离，不取消或统计整页 session
- [x] 3.5 在 Service Worker 中注册经过验证的选区消息处理，并保持 Provider 凭据只在可信上下文可见
- [x] 3.6 添加后台集成测试，覆盖无 Provider、缓存命中、流式成功、认证/限流/网络错误、取消和越权消息
- [x] 3.7 运行 `npm run verify`，确认后台翻译、并发与凭据边界全部通过

## 4. 构建页面魔法镜交互

- [x] 4.1 先为 trigger、loading、streaming、complete、error 和 hidden 状态转换编写 controller 测试
- [x] 4.2 实现指针与键盘选区稳定监听，保证检测阶段只创建本地快照且不发送远端请求
- [x] 4.3 实现单实例 closed Shadow DOM host、A3 风格翻译按钮和轻量卡片视觉结构
- [x] 4.4 实现点击后固定快照、立即 loading、流式纯文本渲染、完成和安全错误反馈
- [x] 4.5 实现按选区最后一个非空 rect 锚定、视口翻转/限制以及滚动和 resize 的单帧重定位
- [x] 4.6 实现复制译文、原文换行加译文的复制双语、复制失败反馈和重新翻译操作
- [x] 4.7 实现关闭、外部点击、`Esc`、新选区和页面隐藏的取消与旧 `requestId` 失效处理
- [x] 4.8 将 magic-lens controller 接入 Content Script 启动流程，且不写入 `currentSession.blocks`
- [x] 4.9 运行 `npm run verify`，确认页面状态机和现有悬浮控制器均无回归

## 5. 验证隔离、可访问性与真实页面体验

- [x] 5.1 添加测试确认输入控件、`contenteditable`、`pre`、`code`、扩展译文和魔法镜自身选区不显示入口
- [x] 5.2 添加整页翻译共存测试，覆盖独立进度、停止、恢复原文、取消和迟到响应
- [x] 5.3 添加可访问性测试，覆盖原生按钮、可见焦点、`aria-live`、键盘进入和 `Esc` 关闭
- [x] 5.4 添加性能测试，锁定按钮快速出现、点击后立即 loading、缓存命中目标 300ms 和重定位不抖动
- [x] 5.5 在真实文章网站验证短词、长句、长段落边界、滚动、窗口边缘、亮色/暗色页面、慢响应和错误恢复
- [x] 5.6 记录 BrowserOS 实测步骤与结果，并确认扩展错误页和 Service Worker 日志无凭据或上下文明文
- [x] 5.7 运行 `npm run verify` 并修复所有回归

## 6. 文档与完成检查

- [x] 6.1 更新 README 当前能力、使用说明、隐私边界和第一版不支持范围
- [x] 6.2 更新端到端验收文档，加入魔法镜正常、缓存、重新翻译、取消和共存场景
- [x] 6.3 运行 `openspec validate add-paragraph-magic-lens --strict` 并修复所有规范问题
- [x] 6.4 运行最终 `npm run verify`、`git diff --check` 和敏感信息扫描，确认零已知错误后提交评审

## 1. 锁定现有行为与格式样本

- [x] 1.1 增加代表用户截图的富文本 fixture，覆盖粗体编号标题、多个行内代码片段、普通文本混排、嵌套格式、安全/危险链接、上下标和显式换行
- [x] 1.2 为当前纯文本提取、批量映射、流式首段、安全 `textContent` 渲染、重复响应和恢复原文补充回归断言，确保格式升级不破坏无格式页面
- [x] 1.3 记录富文本块当前丢失格式的失败基线，并运行 `npm run verify` 确认除预期新增断言外既有测试保持通过

## 2. 受限格式模型与提取

- [x] 2.1 新建零依赖共享格式模块，定义格式 schema 版本、mark 数量/长度上限、受支持语义类型和不包含正文或 URL 的结构指纹
- [x] 2.2 实现候选块文本节点遍历，为 `strong/b`、`em/i`、`code`、`kbd`、`mark`、`sub`、`sup`、`br` 和 `a` 建立稳定、可嵌套的块内格式描述
- [x] 2.3 对任意 class、样式、事件属性、自定义元素和不支持的行内容器只展开可见文本；把链接目标留在 Content Script 本地且不进入远端载荷
- [x] 2.4 将格式结构指纹加入 canonical block 键，使相同文本/相同结构继续扇出，相同文本/不同结构不再错误共享
- [x] 2.5 为嵌套语义、重复文本、格式差异、mark 上限、不支持元素和链接本地化补充单元测试，然后运行 `npm run verify`

## 3. 不透明标记协议与 Provider 校验

- [x] 3.1 实现版本化成对标记序列化器，在保留完整句段上下文的同时让 Provider 可移动完整格式片段，并保持无格式块沿用现有纯文本协议
- [x] 3.2 实现共享格式解析器，严格校验版本、mark ID 集合、唯一性、开闭配对、合法嵌套和保留标记长度，不对缺失或未知标记进行猜测
- [x] 3.3 实现安全纯文本降级器，在格式校验失败时移除扩展保留标记但保留可读译文，并拒绝把未知标记解释为 DOM 指令
- [x] 3.4 扩展 Content Script 消息边界，只允许受限格式元数据、结构指纹和版本字段，拒绝 HTML、标签名、DOM 属性、链接地址、样式或请求配置覆盖
- [x] 3.5 更新批量与单段 OpenAI-compatible 提示词和响应校验，要求模型原样保留格式标记，同时保持 block ID 映射、纯文本安全和既有错误分类
- [x] 3.6 为标记重排、嵌套、遗漏、重复、改写、错误闭合、未知 ID、模型 HTML 和普通纯文本响应补充协议测试，然后运行 `npm run verify`

## 4. 安全格式化渲染与流式体验

- [x] 4.1 实现 DOM 白名单工厂，只通过 `createElement`、`createTextNode` 和 `DocumentFragment` 构造受支持格式，禁止使用模型 `innerHTML`
- [x] 4.2 实现安全链接恢复：只从原始格式描述读取目标，允许 HTTP、HTTPS、mailto 和同页 fragment，危险 scheme 降级为普通文本，新窗口链接强制安全 rel
- [x] 4.3 把格式化渲染接入批量结果、canonical 扇出、动态新增内容、失败重试和缓存命中路径；格式失败时写入纯文本并记录不含正文的 `format-fallback`
- [x] 4.4 为格式化首段实现增量标记过滤，流式期间不显示标记碎片，完成后原子升级为格式化 DOM，中断、停止和旧 session 不保留部分结果
- [x] 4.5 更新译文 CSS，使粗体、斜体、行内代码、键盘输入、上下标和链接保持可读且不复制站点任意样式
- [x] 4.6 为嵌套 DOM、HTML 注入、危险链接、重复响应、流式跨 chunk 标记、取消、恢复原文和无格式兼容补充渲染测试，然后运行 `npm run verify`

## 5. 缓存、性能与兼容迁移

- [x] 5.1 提升响应协议、格式模型和 session cache schema 版本，把格式结构指纹加入缓存键，使旧纯文本缓存自然失效且不迁移用户 Provider 配置
- [x] 5.2 仅缓存完成且通过 block/格式校验的序列化结果；区分格式完整与纯文本降级命中，并继续拒绝缓存流式部分结果、原始 DOM、链接地址或页面正文
- [x] 5.3 扩展确定性性能 fixture，验证无格式页面维持既有请求数与首屏预算，富文本解析和最终 DOM 升级不阻塞首批 loading 或首个可见文本
- [x] 5.4 为缓存隔离、格式完整命中、降级命中、旧版本 miss、零请求视口和性能预算补充测试，然后运行 `npm run verify`

## 6. 真实页面验收

- [x] 6.1 在 `https://claude-code-from-source.com/ch01-architecture/` 验证截图对应段落的粗体 “5. Memory” 与 `memdir/`、`CLAUDE.md`、`~/.claude/MEMORY.md` 行内代码样式在中文译文中正确保留
- [x] 6.2 验证格式片段发生中英文语序调整、页面滚动和缓存复用后仍归属正确，并验证停止、再次点击移除翻译和恢复原文不修改原始 DOM
- [x] 6.3 使用包含模型 HTML、畸形标记和危险链接的确定性 Provider 响应做安全验收，确认只产生白名单 DOM 或纯文本降级且扩展错误页无新增错误
- [x] 6.4 运行最终 `npm run verify`、`git diff --check` 和 `openspec validate preserve-translation-formatting --strict`，记录不支持的复杂布局与 Provider 标记遵循度为后续风险

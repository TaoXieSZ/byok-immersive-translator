# 隐私说明

## 扩展会处理什么

当你主动点击“开始翻译”时，扩展会从当前网页提取可见的标题、段落、列表项、引用和部分表格文本，并把这些文本分批发送到你配置的翻译 API。

扩展不会主动发送完整 HTML、Cookie、表单值、输入框内容、代码块或隐藏内容。

## API Token 如何保存

Provider 配置和 API Token 只保存在当前浏览器配置文件的本地扩展存储中，不使用浏览器同步存储，也不会发送给本项目维护者。

Token 只由扩展 Service Worker 读取，用于向你配置的 Provider 添加 Bearer Authorization 请求头。网页中的 Content Script 只能提交经过校验的文本块，不能指定请求地址、模型、认证头或 Token。

## 网络访问

扩展只在以下两种情况下发起网络请求：

- 你在设置页点击“测试连接”；
- 你在网页上开始或重试翻译。

远程 Provider 必须使用 HTTPS。本机开发服务可使用 `http://localhost` 或 `http://127.0.0.1`。扩展按 Provider 的精确 origin 申请权限，不保留已无配置引用的 API 权限。

## 本地安全边界

浏览器本地存储不是密码保险库。能够调试浏览器、读取你的系统账户数据、修改扩展目录或控制浏览器配置文件的人，可能读取 Token。请为翻译创建独立、可撤销、有限额的 Token，不要复用高权限密钥。

## 数据保留

扩展不建立服务器，也不收集遥测。译文只插入当前页面 DOM，刷新页面或点击“恢复原文”后消失。Provider 配置会保留在本地，直到你在设置页删除它或清除浏览器扩展数据。
